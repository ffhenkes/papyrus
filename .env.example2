GOOS=windows
GOARCH=amd64
# Note: For Docker mode (make up), leave OLLAMA_URL commented out to use http://ollama:11434 (internal service)
# For local mode (make run), use http://localhost:11434 
# OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b
PDF_FILE=pdfs/test.pdf
CUSTOM_PROMPT="Explain the purpose of the document."
PAPYRUS_PATH=bin/windows-amd64

